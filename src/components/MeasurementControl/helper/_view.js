/* eslint-disable no-unused-vars */
export class View {
  start() {}
  view({
    coordinates = [],
    features = [],
    value = 0,
    features_label = []
  } = {}) {}
  reset() {}
  destroy() {}
}
